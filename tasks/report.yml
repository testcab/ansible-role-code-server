- name: List installed files
  debug:
    msg:
    - "{{ bin_dir }}/code-server"
    - "{{ systemd_unit_files_dir }}/code-server.service"
    - "{{ systemd_unit_files_dir }}/code-server.service.d/restart.conf"
    - "{% if code_server_password is defined %}{{ code_server_data_dir }}/env{% endif %}"
    - "{% if tls_cert is defined %}{{ code_server_data_dir }}/tls.cert{% endif %}"
    - "{% if tls_key is defined %}{{ code_server_data_dir }}/tls.key{% endif %}"
